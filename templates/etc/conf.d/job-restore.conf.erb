### Job Definition ###
Job {
  Name = "Job-Restore-Files"
  Type = Restore
  Client = Client-<%= @fqdn %>
  FileSet = "UnixServerComplete"
  Storage = File
  Pool = Default
  Full Backup Pool = Full-Pool
  Incremental Backup Pool = Inc-Pool
  Differential Backup Pool = Diff-Pool
  Messages = Standard
  Where = /srv/backup/bacula-restores
}
